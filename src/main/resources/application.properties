
spring.application.name=MoneyManager

# Disable automatic schema generation since we're using Flyway migrations
spring.jpa.hibernate.ddl-auto=validate

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=root
spring.datasource.password=priti
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql:true
server.servlet.context-path=/api/v1.0
spring.jpa.properties.hibernate.format_sql=true

#Email Configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=95c922001@smtp-brevo.com
spring.mail.password=P9nNUOa63VRjwGTC
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT Configuration
# Use a strong secret key (at least 32 characters)
jwt.secret=MoneyManagerSecretKey2024ThisIsAStrongSecretKeyForJWT
# Token expiration in milliseconds (24 hours)
jwt.expiration=86400000

# Mail Configuration
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=vaibhavsable150@gmail.com


money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL:http://localhost:5173}

spring.profiles.active=prod

app.activation.url=${MONEY_MANAGER_BACKEND_URL:http://localhost:8080}